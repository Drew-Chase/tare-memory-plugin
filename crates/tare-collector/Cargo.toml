[package]
name = "tare-collector"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Runtime tracking allocator + sampled backtrace capture"

[dependencies]
tare-schema = { path = "../tare-schema" }
