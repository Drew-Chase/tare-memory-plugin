[package]
name = "tare-schema"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "JSON contract types for tare allocation data"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
