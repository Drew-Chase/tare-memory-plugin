[package]
name = "xtask"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "cargo xtask orchestration: static / profile / all"

[dependencies]
tare-schema = { path = "../tare-schema" }
