[package]
name = "tare-aggregate"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Merges runtime + static into one JSON report with content hashes"

[dependencies]
tare-schema = { path = "../tare-schema" }
